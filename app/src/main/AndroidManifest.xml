<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sentimenmrt">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MRTweets"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".realtimechart"></activity>
        <activity android:name=".menuchart" />
        <activity android:name=".About" />
        <activity android:name=".Pie" />
        <activity android:name=".Wordcloud" />
        <activity android:name=".Table" />
        <activity android:name=".grafik" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>